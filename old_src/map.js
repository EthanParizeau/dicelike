class Map {
    constructor(tiles, player) {
        this.player = player;
        this.tiles = tiles;
        this.entities = [];
    }

    getTile(x, y) {
        console.log("TILE");
    }
}

export default Map;